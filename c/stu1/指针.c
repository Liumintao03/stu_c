#include <stdio.h>


// int main(){

//     int a =10;
//     printf("%d\n",a);
//     //p是指针变量，p存放的地址是指针，指针就是地址
//     int *p = &a;//&是找这个变量的地址
//     *p = 20;//*是解引用，通过这个地址找到变量，此时存放在p的地址找到的变量是a
//     printf("%d\n",a);

//     printf("%d\n",p);//可以看出，p存放的是地址
//     printf("%d\n",*p);//*p找到的是p存放地址的a

//     return 0;
// }